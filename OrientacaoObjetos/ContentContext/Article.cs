namespace OrientacaoObjetos.ContentContext
{
     public class Article: Content
     {
        //Classe vazia, não tem props mas herda as  props do content.
        public Article(string title, string url) :base(title,url)
        {
         
        }
     }
}